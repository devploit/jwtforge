# @vercel/speed-insights declares @sveltejs/kit as an OPTIONAL peer; npm tries
# to resolve that optional peer's tree (vite@8) and conflicts with vitest's
# vite@5. We use none of the SvelteKit path, so relax strict peer resolution.
legacy-peer-deps=true
